#!/bin/sh

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

echo "Running guardian pre-commit..."
cd "$PROJECT_ROOT"

python3 -m guardian.cli scan --path .
python3 -m guardian.cli validate .

echo ""
echo "All pre-commit hooks passed!"
exit 0
