# Agent 上下文

本文档是 **nano-coding-agent** 在当前工程中的常驻上下文。它定义了本项目的治理规则、协作范式与行为边界，是 AI Agent 与人类开发者共同遵守的“项目宪法”。

---

## 定位

**nano-coding-agent** 是一个轻量级的项目级原则守护者。它通过 CLI 命令与 Git Hook 的组合，在编码流程的关键节点自动执行治理检查，确保：

- 代码变更符合 `AGENTS.md` 规范
- 测试先行（TDD）得到落实
- 安全与审计问题在提交前被发现
- 核心原则文档与代码库同步演化

本 Agent 采用**本地优先**架构：全局 CLI 负责调度，而实际的配置、技能、原则与钩子优先从当前工程的 `.nano-coding-agent/` 目录加载。这意味着每个项目都可以定制自己的检查逻辑，Agent 的能力会跟随项目一起成长。

---

## 核心技能与作用效果

### 1. `validate` — 项目健康检查
验证项目是否满足基础治理规则：
- `AGENTS.md` 存在且包含 `## 基础原则`
- 不存在禁止的根目录文件
- 单一源文件不超过 1000 行
- 已建立 `tests/unit/` 与 `tests/integration/` 目录分离
- README.md / AGENTS.md 中记录了明确的测试命令

### 2. `scan` — 安全与审计扫描
在代码提交前运行 Semgrep 规则与正则审计模式，识别：
- 安全漏洞（如敏感信息泄露、危险的动态执行）
- 审计警告（如空 catch 块、硬编码凭据、过长的函数）
扫描规则可通过 `.nano-coding-agent/config.yaml` 进行项目级扩展。

### 3. `update` — 原则同步
将 `.nano-coding-agent/principles/` 下的原则块自动合并到 `AGENTS.md`。通过语义相似度去重，并依据技能实现与 CI/CD 集成情况，自动为实践条目打上 `[suggest]`、`[support]` 或 `[control]` 标签。保证原则文档始终反映最新的工程实践。

### 4. `principle-review` — 原则合规审查
对核心指导原则文档进行深度审查：
- `--check-changes`：监控 `AGENTS.md`、`README.md` 等文件的未提交变更，要求人工确认
- `--separate-concerns`：检查 `AGENTS.md` 是否正确使用 `NANO_CODING_GENERATED` 标记与分隔符
- `--length-limit`：对超过 1000 行的文档或超过 150 行的原则块发出告警

---

## 协作范式

### Git Hook 集成
安装 Agent 后，`.git/hooks/pre-commit` 会自动调度 `.nano-coding-agent/hooks/pre-commit`：
1. 运行 `scan` 扫描变更文件
2. 运行 `validate` 检查项目健康度
3. 若存在阻塞性问题，阻止本次提交

### 多环境分支策略（推荐）
建议采用四级分支模型，确保代码质量逐级验证：

| 分支 | 用途 | 合并方向 |
|------|------|----------|
| `DEV` | 新特性初步尝试 | → `ST` |
| `ST` | 自动化测试/边界条件验证 | → `UAT` |
| `UAT` | 人工验收/检核 | → `main(PRD)` |
| `main(PRD)` | 正式生产环境 | - |

### 本地 Skill 优先
你可以在本项目的 `.nano-coding-agent/skills/` 目录下新增或覆盖技能。当 CLI 检测到同名技能时，项目本地版本会优先于内置版本执行。这让本项目能够：
- 扩展自定义检查规则
- 覆盖默认命令的行为
- 在 `.nano-coding-agent/` 内闭环演化项目专属的 Agent 能力

---

## 自定义与扩展

编辑 `.nano-coding-agent/config.yaml` 可以调整：
- `validate` 的忽略目录与最大行数限制
- `scan` 启用的审计工具与自定义规则路径
- 其他项目级偏好设置

配置采用**三级合并**：项目配置 > 用户目录配置（`~/.nano-coding-agent/config.yaml`）> 内置默认值。

---

## 版本兼容

`.nano-coding-agent/version` 记录了初始化时的 CLI 版本。当全局 CLI 升级后，若检测到版本不匹配，会在 stderr 输出 WARNING 提示你可能需要重新运行 `install` 以同步最新模板和 hooks，但**不会中断**当前命令的执行。
